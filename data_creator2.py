
leftRange = -0.000110
rightRange = +0.000110
topRange = +0.000090
bottomRange = -0.000090

define = 0
firstLatitude = 35.153270
firstLongitude = 129.131930
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange

define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += rightRange
define = 0
firstLatitude = lastLatitude + bottomRange
firstLongitude = lastLongitude
lastLatitude = 0.0
lastLongitude = 0.0
arrLen = 20
for i in range(arrLen):
    print('{0:.6f}'.format(firstLatitude) + ',' + '{0:.6f}'.format(firstLongitude + define) + ',')
    if i+1 == arrLen:
        lastLatitude = firstLatitude
        lastLongitude = firstLongitude + define
    define += leftRange
